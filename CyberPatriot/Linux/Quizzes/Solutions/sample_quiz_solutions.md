# Linux Security Quiz - Sample Solutions

1. `cat /etc/passwd`
2. `find / -type f -perm -o+w 2>/dev/null`
3. UFW is the Uncomplicated Firewall, used to manage firewall rules.
4. Set `PermitRootLogin no` in `/etc/ssh/sshd_config`
5. `find / -perm -4000 -type f 2>/dev/null`
